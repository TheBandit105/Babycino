package babycino;

// The kinds (types of types) in MiniJava.
public enum Kind {
    // Array of integers.
    INTARRAY,
    // Booleans.
    BOOLEAN,
    // Integers.
    INT,
    // Any objects (non-primitive types).
    OBJECT;
}

